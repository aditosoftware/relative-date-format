package de.adito.relativedateformat.token;

import java.time.Period;

/** Describes a {@link IExpressionToken} which always has {@link Period} as value type. */
public interface IPeriodExpressionToken extends IExpressionToken<Period> {}
